# System Architecture Requirements
All calculation modules and interactive scripts MUST be built using a REPL (Read-Eval-Print Loop) architecture for terminal execution. 
Under no circumstances should graphical user interfaces (GUIs) like Tkinter or PyQt be used.